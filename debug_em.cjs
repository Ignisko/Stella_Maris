const { chromium } = require('playwright'); (async () => { const browser = await chromium.launch(); const page = await browser.newPage(); page.on('pageerror', err => console.log('PAGE ERROR:', err)); page.on('console', msg => { if(msg.type() === 'error') console.log('CONSOLE ERROR:', msg.text()); }); await page.goto('http://localhost:5173/'); await page.waitForTimeout(2000); for(let i=0; i<11; i++){ const btn = page.locator('button', { hasText: 'Next' }).or(page.locator('button', { hasText: 'Finish' })).first(); if(await btn.isVisible()){ await btn.click(); await page.waitForTimeout(1000); } } await page.waitForTimeout(3000); await browser.close(); })();
